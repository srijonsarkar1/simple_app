from setuptools import setup, find_packages

setup(
    name = "src",
    version = "0.0.1",
    description = "Its wine Q package",
    author = "Srijon",
    packages=find_packages(),
    license="MIT"
)